def vertical_total_stress(layer_thicknesses, layer_densities):
    return sum(t * d for t, d in zip(layer_thicknesses, layer_densities))
print(f"Total stress at base: {vertical_total_stress([2.0, 3.5], [17.5, 19.0]):.2f} kPa")
