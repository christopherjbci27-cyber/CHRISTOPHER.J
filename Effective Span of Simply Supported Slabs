def effective_slab_span(clear_span, eff_depth, support_width):
    return min(clear_span + eff_depth, clear_span + support_width)
print(f"Effective Span: {effective_slab_span(4.0, 0.15, 0.23):.3f} m")
